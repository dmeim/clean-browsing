{
  "name": "clean-newtab",
  "version": "0.4.0",
  "description": "A clean, customizable Chrome extension that transforms your new tab into a powerful dashboard with tile-based widgets and universal website embedding",
  "author": "Dimitri Meimaridis",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/DimitriMeimaridis/clean-newtab.git"
  },
  "bugs": {
    "url": "https://github.com/DimitriMeimaridis/clean-newtab/issues"
  },
  "homepage": "https://github.com/DimitriMeimaridis/clean-newtab#readme",
  "keywords": [
    "chrome-extension",
    "new-tab",
    "dashboard",
    "widgets",
    "customizable",
    "productivity",
    "browser-extension",
    "vanilla-js"
  ],
  "scripts": {
    "test": "echo 'Running extension validation tests' && node -e \"const manifest = require('./extension/manifest.json'); console.log('Manifest valid:', manifest.name, manifest.version);\"",
    "validate": "node -e \"require('./extension/manifest.json'); console.log('Extension structure validated');\"",
    "build": "echo 'No build process required - pure vanilla JS extension'",
    "dev": "echo 'Load extension/ folder in Chrome extensions (chrome://extensions)'",
    "package": "cd extension && zip -r ../dist/clean-newtab-v$(node -p \"require('./manifest.json').version\").zip .",
    "lint": "echo 'No linter configured - using browser dev tools for validation'"
  },
  "private": true
}
